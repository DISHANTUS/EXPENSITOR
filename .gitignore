# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/
build/
dist/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment
.env
*.env.local

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log

# Docker
docker-compose.override.yml

# Full-state backups — real user emails + password hashes; never commit
backend/backups/

# Built APKs (large binaries / build artifacts)
*.apk

# LLM bridge tunnel binary (downloaded at runtime)
scripts/cloudflared.exe

# Paper: PNG figure previews are regenerated by build_assets.py; the paper uses
# the committed PDFs. (Tables, figure PDFs, and results.json ARE committed so the
# paper compiles in Overleaf with no local run.)
paper/figures/*.png
